exports.handler=async(event)=>{if(event.httpMethod!=='POST')return{statusCode:405,body:JSON.stringify({error:'Method not allowed'})};try{const b=JSON.parse(event.body||'{}');if(!b.prompt)return{statusCode:400,body:JSON.stringify({error:'Prompt is required'})};if(!process.env.REPLICATE_API_TOKEN)return{statusCode:503,body:JSON.stringify({error:'Video engine is not connected. Add REPLICATE_API_TOKEN in Netlify Environment Variables.'})};return{statusCode:501,body:JSON.stringify({error:'Provider adapter needs a configured video model endpoint.'})}}catch(e){return{statusCode:500,body:JSON.stringify({error:e.message})}}};
